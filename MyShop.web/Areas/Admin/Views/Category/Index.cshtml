@model IEnumerable<Category>

@{
    ViewBag.PageTitle = "Category Management";
    ViewBag.CardTitle = "View Categories";
    Layout = "~/Views/Shared/_Dashbord.cshtml";
}

<div>
    <a class="btn btn-primary" asp-action="create">Create</a>
</div>
<hr />

<div class="table-responsive">
    <table class="table table-hover">
        <thead class="text-bold">
            <tr class="text-center">
                <th scope="col">Image</th>
                <th scope="col">Name</th>
                <th scope="col">Description</th>
                <th scope="col">Time Created</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="text-center">
                    <td>
                        <img src="~/@item.Image" class="img-fluid" style="height:35px; width:35px;" alt="@item.Name" />
                    </td>
                    <td>@item.Name</td>
                    <td>@item.Description</td>
                    <td>@item.CreatedAt.ToString("g")</td>
                    <td>
                        <a class="btn btn-success btn-sm" asp-route-id="@item.Id" asp-action="Edit">Edit</a>
                        <a class="btn btn-danger btn-sm" asp-route-id="@item.Id" asp-action="Delete">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
